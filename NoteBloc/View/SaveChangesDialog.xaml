<Window
    x:Class="NoteBloc.SaveChangesDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="NoteBloc"
    Width="400"
    Height="200">
    <Grid>
        <!--  TextBlock centré  -->
        <TextBlock
            x:Name="txtMessage"
            Margin="10"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            TextWrapping="Wrap" />

        <!--  StackPanel pour les boutons, centrée avec un espacement vertical  -->
        <StackPanel
            Margin="10"
            HorizontalAlignment="Center"
            VerticalAlignment="Bottom"
            Orientation="Horizontal">
            <Button
                Width="100"
                Margin="0,0,10,0"
                Padding="10"
                Content="Enregistrer" />
            <Button
                Width="130"
                Margin="0,0,10,0"
                Padding="10"
                Content="Ne pas enregistrer" />
            <Button
                Width="100"
                Padding="10"
                Content="Annuler" />
        </StackPanel>
    </Grid>
</Window>
